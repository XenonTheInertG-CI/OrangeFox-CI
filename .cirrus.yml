env:
    # OrangeFox Sync Repository
    FOX_SYNC: https://gitlab.com/OrangeFox/sync.git

    # Config
    CONFIG: config.sh

    # Telegram
    TG_CHAT_ID: -1001754984978
    TG_TOKEN: 6535373929:AAGjmHIWwzFRL_hVRDUKDFHXmMpwd56XAck

    # Cirrus Specific Vars (Do *NOT* Change)
    CIRRUS_SHELL: bash

task:
  name: "OrangeFox CI"
  timeout_in: 120m
  container:
      image: ghcr.io/sushrut1101/docker:latest
      cpu: 8
      memory: 20G
      
  Checks_script:
      - curl -sL https://raw.githubusercontent.com/axsrog/OrangeFox-CI/main/scripts/checks.sh | bash

  Sync_script:
      - curl -sL https://raw.githubusercontent.com/axsrog/OrangeFox-CI/main/scripts/sync.sh | bash

  Build_script:
      - curl -sL https://raw.githubusercontent.com/axsrog/OrangeFox-CI/main/scripts/build.sh | bash

  Upload_script:
      - export TIMEOUT=43200 # Link timeout (in minutes)
      - curl -sL https://raw.githubusercontent.com/axsrog/OrangeFox-CI/main/scripts/upload.sh | bash
